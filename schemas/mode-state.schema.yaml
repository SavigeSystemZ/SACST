$schema: "http://json-schema.org/draft-07/schema#"
type: object
properties:
  mode:
    type: string
    enum: [advisor, operator, yolo]
  requires_confirmation_for_critical: { type: boolean }
  auto_log_actions: { type: boolean }