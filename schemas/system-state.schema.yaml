$schema: "http://json-schema.org/draft-07/schema#"
type: object
properties:
  instance: { type: string }
  host: { type: string }
  root: { type: string }
  platform_class: { type: string }
  collected_at_utc: { type: string }
  latest_raw_audit_dir: { type: string }
  normalized_context_dir: { type: string }