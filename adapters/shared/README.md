# Shared Adapter Inputs

Shared adapter inputs define the common policy that generated tool-specific adapters must reflect.

Current generator behavior is implemented in `bin/sys-generate-adapters`. Project adapter files are regenerated from canonical docs plus project metadata and normalized truth.
